/* final 사용
 * - 클래스 앞에 붙으면 상속 불가!
 * - 메서드 앞에 붙으면 오버라이딩 불가!
 * - 변수 앞에 붙으면 값을 한 번만 할당!
 */
package step16.ex6;

// 클래스 앞에 final을 붙이면 상속 불가!
// => 기능을 변경하지 못하게 하기 위해 상속을 막는다.
public final class MyClass {}
